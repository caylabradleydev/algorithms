// "Hello, World!"
console.log("Hello, World!"); //instructs the console to print the string "Hello, World!" to the console

// `console`: Refers to the JavaScript console, a built-in object in web browsers and some JS environments that provides a way for developers to output messages, warnings, and errors for debugging and development purposes

// `.log()`: a method of the `console` object that can take one or more arguments and print them to the console

// `("Hello, World!)`: The argument, in this case a String, passed to the `log()` method and printed to the console
