// "Hello World"

using System; //namespace which contains fundamental types and the `Console` class for input and output operations.

class Program // Defines a class named `Program`.
{
  static void Main() //Declares the entry point of the program, where the `Main` method is called when the program starts.
  {
    Console.WriteLine("Hello, World"); //outputs the string "Hello, World!" followed by a newline to the console.
  }
};
