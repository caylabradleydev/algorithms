// "Hello, World!"

package main //Specifies that this Go file is part of the `main` package, which is the starting point for the execution of the program

import "fmt" //Imports the `fmt` package, which provide functions for formatted I/O (including Println())

func main() { //Declares the `main` function, which is the entry point of the program
	fmt.Println("Hello, World!") //Outputs the string "Hello, World!" followed by a newline to the console.
}
