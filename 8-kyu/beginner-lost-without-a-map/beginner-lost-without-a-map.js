function maps(x){
  //NOTES
  //input: array of integers
  //output: NEW array with each value doubled
  
  //SOLUTION 1 (clean solution)
  //map through given array, doubling each time
  return x.map((y) => y * 2)
  
  //SOLUTION 2 (efficient solutoin for larger datasets)
  //initialize new output array
  //const output = []
  
  //iterate through given array
//   for(let i = 0; i < x.length; i++){
//     output.push(x[i] * 2)
//   }
  
//   return output
}